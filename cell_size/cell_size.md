At some point while working on my master thesis, I noticed that I had used inconsistent values for the pbc cell size.

Some input files had the cell size defined as:
(...)

While others had it defined as:
(...)

